name: Poetry
repository: https://github.com/python-poetry/poetry
category: python-packaging
replaces:
  - tool: pipenv
    fit: full
    note: Dependencies and lockfile in pyproject.toml instead of a Pipfile.
