name: git-cliff
repository: https://github.com/orhun/git-cliff
category: changelog
replaces:
  - tool: semantic-release
    fit: partial
    note: Changelog generation only, no version bump or publish.
  - tool: conventional-changelog
    fit: full
    note: Generates changelogs from Conventional Commits with its own templates, as a single binary.
