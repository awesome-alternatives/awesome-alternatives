name: Sealed Secrets
repository: https://github.com/bitnami/sealed-secrets
category: encrypted-files
replaces:
  - tool: sops
    fit: partial
    note: Kubernetes Secrets only, encrypted against a key held by the cluster controller and decrypted there.
