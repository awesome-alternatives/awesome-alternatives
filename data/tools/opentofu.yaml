name: OpenTofu
repository: https://github.com/opentofu/opentofu
category: infrastructure-as-code
replaces:
  - tool: terraform
    fit: drop-in
    note: Fork of Terraform 1.5, reads the same configuration and state.
