name: Hugo
repository: https://github.com/gohugoio/hugo
category: static-site-generator
replaces:
  - tool: jekyll
    fit: full
    note: Single binary with much faster builds.
  - tool: hexo
    fit: full
    note: A single binary that builds large sites in seconds.
