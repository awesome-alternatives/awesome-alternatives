name: OpenHands
repository: https://github.com/OpenHands/OpenHands
category: coding-agent
replaces:
  - tool: claude-code
    fit: full
    note: Agent that works inside a sandboxed container, driven from a web UI, a CLI or CI.
  - tool: github-copilot
    fit: partial
    note: An agent that works on the whole repository, without completions as you type.
