name: Gogs
repository: https://github.com/gogs/gogs
category: git-forge
replaces:
  - tool: github
    fit: partial
    note: Repositories, issues and pull requests in a single binary, without built-in CI.
  - tool: bitbucket
    fit: partial
    note: Repositories and pull requests, without built-in CI.
deploy: [container, binary]
