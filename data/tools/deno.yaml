name: Deno
repository: https://github.com/denoland/deno
category: javascript-runtime
replaces:
  - tool: nodejs
    fit: full
    note: Runs most npm packages through its Node compatibility layer.
  - tool: ts-node
    fit: full
    note: Runs TypeScript files directly, with no separate compile step.
