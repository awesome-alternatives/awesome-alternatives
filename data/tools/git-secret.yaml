name: git-secret
repository: https://github.com/sobolevn/git-secret
category: encrypted-files
replaces:
  - tool: git-crypt
    fit: partial
    note: Encrypts files with the GPG keys of each allowed user, through explicit hide and reveal commands.
