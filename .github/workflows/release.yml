name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    name: Release
    if: ${{ !startsWith(github.event.head_commit.message, 'chore(release)') }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      tags: ${{ steps.tags.outputs.tags }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - name: Set the release commit author
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
      - uses: FerrLabs/FerrFlow@9a7a1901c1f7bc964d5765bfe480eb333a0c791b # v7.22.0
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          version: '7.22.0'
      - name: List the tags this release created
        id: tags
        run: |
          tags=$(git tag --points-at HEAD | grep -E '^(api|site)-v' | jq -Rsc 'split("\n") | map(select(length > 0))')
          echo "tags=$tags" >> "$GITHUB_OUTPUT"
          echo "Released: $tags"

  image:
    name: Image ${{ matrix.tag }}
    needs: release
    if: ${{ needs.release.outputs.tags != '' && needs.release.outputs.tags != '[]' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        tag: ${{ fromJSON(needs.release.outputs.tags) }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ matrix.tag }}
          persist-credentials: false
      - name: Build and push
        env:
          TAG: ${{ matrix.tag }}
          TOKEN: ${{ github.token }}
          ACTOR: ${{ github.actor }}
          OWNER: ${{ github.repository_owner }}
          SOURCE: ${{ github.server_url }}/${{ github.repository }}
        run: |
          name="${TAG%%-v*}"
          version="${TAG#*-v}"
          image="ghcr.io/${OWNER}/${name}"
          case "$name" in
            api) context=api; file=api/Dockerfile ;;
            site) context=.; file=site/Dockerfile ;;
          esac
          echo "$TOKEN" | docker login ghcr.io -u "$ACTOR" --password-stdin
          docker build -f "$file" \
            --label "org.opencontainers.image.source=$SOURCE" \
            --label "org.opencontainers.image.version=$version" \
            -t "$image:$version" -t "$image:latest" "$context"
          docker push "$image:$version"
          docker push "$image:latest"
